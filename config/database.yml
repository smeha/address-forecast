default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: address_forecast_development

test:
  <<: *default
  database: address_forecast_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
